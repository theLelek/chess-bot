standard uci features

plus special commands:
"get legal moves"    -> returns the legal moves in the current position
"go fen"    -> returns a fen string after the engine made the best move

the Engine returns an Error message in the Format: "ERROR " + theErrorMessage
